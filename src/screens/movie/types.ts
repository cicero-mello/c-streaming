import { FakeVideoProps, SuggestionMedias } from "../../components"

export interface PageMediaProps {
    fakeVideo: FakeVideoProps
    mediaTitle: string
    sinopsys: string
    id: string
    suggestionMedias: SuggestionMedias[]
}
