export enum PATHS {
    HOME = "/",
    SEARCH = "/search",
    USER = "/user",
    MOVIE = "/movie",
    SERIES = "/series",
    WATCH_LATER = "/watch-later",
    HISTORY = "/history",
    ERROR = "/404"
}
